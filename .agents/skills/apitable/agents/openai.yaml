interface:
  display_name: "Apitable"
  short_description: "Repo-specific patterns and workflows for apitable"
  default_prompt: "Use the apitable repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true